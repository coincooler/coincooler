var delay = parseInt("<%= FLASH_DELAY_SECONDS*1000 %>");
var fade = parseInt("<%= FLASH_FADE_SECONDS*1000 %>");

var slow = delay * 1000 // this is to prevent fadeout on pk page

// $('.alert.alert-success').delay(delay).fadeOut(fade);
// $('.alert.alert-danger').delay(delay).fadeOut(fade);
// $('.alert.alert-error').delay(delay).fadeOut(fade);