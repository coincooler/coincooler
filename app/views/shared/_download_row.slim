- unless params[:controller]=='freezers'
	td.download
		= link_to image_tag('arrow.png', size: "16x10", alt: download_row_button),download_row_path(download: row),class: "btn btn-primary download",title: "Download a file containing just the Private Key and Address in the #{row.to_i.ordinalize} row", id: "download_row_#{row}", remote: @remote
= javascript_include_tag 'download_rows' if @remote